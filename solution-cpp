#include <iostream>

int main()
{
	unsigned sum = 0;
	unsigned lim = 0;
	std::cin >> lim;
	for(auto i=0; i<lim; ++i) 
		if(!(i%3) || !(i%5)) sum+=i;
	std::cout << "Result: " << sum << std::endl;
	return 0;
}
